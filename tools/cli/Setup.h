#pragma once

#include <string>
#include <vector>

namespace tool {

int cmdFetchAssets(const std::vector<std::string>& args);
int cmdNewGame(const std::vector<std::string>& args);

} // namespace tool
